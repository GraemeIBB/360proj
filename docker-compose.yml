services:
  backend:
    build: ./backend
    ports:
      - "8800:8800"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "4173:4173"
    depends_on:
      - backend
